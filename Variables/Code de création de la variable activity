from(bucket: "activity")
  |> range(start: -90d)
  |> filter(fn: (r) => r["_measurement"] == "activity")
  |> keep(columns: ["label"])
  |> distinct(column: "label")
  |> group()
  |> distinct(column: "label")


